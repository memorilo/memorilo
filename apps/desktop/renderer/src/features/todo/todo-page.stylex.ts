import * as stylex from '@stylexjs/stylex'

const colors = {
  accent: 'var(--ui-accent, rgb(0, 122, 255))',
  accentForeground: 'var(--ui-accent-foreground, rgb(0, 122, 255))',
  canvas: 'var(--ui-canvas, rgb(255, 255, 255))',
  canvasSolid: 'var(--ui-canvas, rgb(255, 255, 255))',
  controlHover: 'var(--ui-control-hover, rgba(60, 60, 67, 0.08))',
  controlPressed: 'var(--ui-control-pressed, rgba(60, 60, 67, 0.14))',
  controlShadow: 'var(--ui-control-shadow, 0 1px 2px rgba(35, 42, 52, 0.08))',
  divider: 'var(--ui-divider, rgba(60, 60, 67, 0.14))',
  focus: 'var(--ui-focus, rgba(0, 122, 255, 0.72))',
  onAccent: 'var(--ui-on-accent, rgb(255, 255, 255))',
  surface: 'var(--ui-surface, rgb(255, 255, 255))',
  text: 'var(--ui-text, rgba(28, 28, 30, 0.94))',
  textMuted: 'var(--ui-text-muted, rgba(60, 60, 67, 0.64))',
  textQuiet: 'var(--ui-text-quiet, rgba(60, 60, 67, 0.46))',
} as const

const loadingRotation = stylex.keyframes({
  to: { transform: 'rotate(360deg)' },
})

export const todoPageStyles = stylex.create({
  page: {
    position: 'relative',
    display: 'flex',
    width: '100%',
    height: '100%',
    minWidth: 0,
    minHeight: 0,
    overflow: 'hidden',
    paddingTop: 56,
    backgroundColor: {
      'default': colors.canvas,
      '@media (prefers-reduced-transparency: reduce)': colors.canvasSolid,
      '@media (prefers-contrast: more)': 'rgb(255, 255, 255)',
    },
    color: colors.text,
    boxShadow: 'none',
  },
  content: {
    display: 'flex',
    width: '100%',
    minWidth: 0,
    minHeight: 0,
    flex: 1,
    flexDirection: 'column',
    paddingTop: 0,
    paddingRight: 0,
    paddingBottom: 0,
    paddingLeft: 0,
  },
  titlebarViews: {
    display: 'flex',
    alignItems: 'center',
    gap: 1,
    borderColor: 'var(--ui-field-border, rgba(255, 255, 255, 0.9))',
    borderStyle: 'solid',
    borderWidth: 1,
    borderRadius: 'var(--ui-surface-radius, 16px)',
    padding: 2,
    backgroundColor: 'var(--ui-surface-translucent, rgba(255, 255, 255, 0.46))',
    backgroundImage: 'none',
    boxShadow: 'var(--ui-shadow-raised, 0 8px 24px rgba(28, 28, 30, 0.12))',
  },
  titlebarViewOption: {
    display: 'grid',
    width: 31,
    height: 26,
    placeItems: 'center',
    borderWidth: 0,
    borderRadius: 'var(--ui-control-radius, 13px)',
    padding: 0,
    backgroundColor: {
      'default': 'transparent',
      ':hover': colors.controlHover,
      ':active': colors.controlPressed,
    },
    color: colors.textMuted,
    cursor: 'default',
    outline: 'none',
    boxShadow: {
      'default': 'none',
      ':focus-visible': `0 0 0 2px ${colors.focus}`,
    },
    transform: {
      'default': 'scale(1)',
      ':active': 'scale(0.94)',
    },
    transitionDuration: {
      'default': '100ms',
      '@media (prefers-reduced-motion: reduce)': '0ms',
    },
    transitionProperty: 'background-color, box-shadow, transform',
    transitionTimingFunction: 'ease-out',
  },
  titlebarViewOptionSelected: {
    backgroundColor: colors.accent,
    backgroundImage: 'none',
    boxShadow: colors.controlShadow,
    color: colors.onAccent,
  },
  controls: {
    display: 'flex',
    minHeight: 48,
    flex: '0 0 auto',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: 16,
    borderBottomColor: colors.divider,
    borderBottomStyle: 'solid',
    borderBottomWidth: 1,
    paddingRight: 18,
    paddingLeft: 18,
  },
  listControlsTitle: {
    display: 'flex',
    minWidth: 0,
    alignItems: 'center',
    gap: 10,
  },
  listSidebarToggle: {
    display: 'grid',
    width: 30,
    height: 30,
    flex: '0 0 auto',
    placeItems: 'center',
    borderColor: 'var(--ui-field-border, rgba(60, 60, 67, 0.18))',
    borderStyle: 'solid',
    borderWidth: 1,
    borderRadius: 'var(--ui-control-radius, 13px)',
    padding: 0,
    backgroundColor: 'var(--ui-control-background, transparent)',
    backgroundImage: 'none',
    color: colors.textMuted,
    cursor: 'default',
    outline: 'none',
    boxShadow: {
      'default': 'none',
      ':hover': 'var(--ui-control-shadow, 0 1px 2px rgba(35, 42, 52, 0.08))',
      ':focus-visible': `0 0 0 2px ${colors.focus}`,
    },
    transform: {
      'default': 'scale(1)',
      ':active': 'scale(0.95)',
    },
    transitionDuration: {
      'default': '100ms',
      '@media (prefers-reduced-motion: reduce)': '0ms',
    },
    transitionProperty: 'background-color, box-shadow, color, transform',
    transitionTimingFunction: 'ease-out',
  },
  listTitle: {
    overflow: 'hidden',
    margin: 0,
    color: colors.accentForeground,
    fontSize: 20,
    fontWeight: 720,
    letterSpacing: '-0.018em',
    lineHeight: '26px',
    textOverflow: 'ellipsis',
    whiteSpace: 'nowrap',
  },
  count: {
    display: 'flex',
    alignItems: 'center',
    gap: 6,
    margin: 0,
    color: colors.textMuted,
    fontSize: 12,
    fontVariantNumeric: 'tabular-nums',
    fontWeight: 500,
    lineHeight: '18px',
  },
  countLoadingIcon: {
    width: 13,
    height: 13,
  },
  listLayout: {
    position: 'relative',
    display: 'flex',
    width: '100%',
    height: '100%',
    minWidth: 0,
    minHeight: 0,
    overflow: 'hidden',
  },
  listSidebarMotion: {
    'position': 'relative',
    'zIndex': 5,
    'height': '100%',
    'minHeight': 0,
    'overflow': 'hidden',
    'flex': '0 0 auto',
    '@media (max-width: 980px)': {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: 0,
      boxShadow: '14px 0 34px rgba(31, 38, 48, 0.14)',
    },
  },
  listSidebarScrim: {
    'position': 'absolute',
    'zIndex': 4,
    'display': 'none',
    'inset': 0,
    'borderWidth': 0,
    'padding': 0,
    'backgroundColor': 'rgba(31, 35, 42, 0.08)',
    '@media (max-width: 980px)': {
      display: 'block',
    },
  },
  listMain: {
    display: 'flex',
    minWidth: 0,
    minHeight: 0,
    overflow: 'hidden',
    flex: 1,
    flexDirection: 'column',
    paddingLeft: 0,
  },
  viewRegion: {
    position: 'relative',
    display: 'flex',
    minWidth: 0,
    minHeight: 0,
    overflow: 'hidden',
    flex: 1,
    backgroundColor: colors.surface,
  },
  status: {
    display: 'grid',
    width: '100%',
    height: 'clamp(240px, 52vh, 520px)',
    placeItems: 'center',
    color: colors.textMuted,
  },
  statusContent: {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column',
    gap: 8,
    fontSize: 13,
    fontWeight: 500,
    textAlign: 'center',
  },
  statusGlyph: {
    width: 22,
    height: 22,
    color: colors.textQuiet,
  },
  loadingIcon: {
    'animationDuration': '850ms',
    'animationIterationCount': 'infinite',
    'animationName': loadingRotation,
    'animationTimingFunction': 'linear',
    '@media (prefers-reduced-motion: reduce)': {
      animationName: 'none',
    },
  },
  errorIcon: {
    color: 'rgb(184, 83, 69)',
  },
  retryButton: {
    height: 28,
    borderColor: colors.accent,
    borderStyle: 'solid',
    borderWidth: 1,
    borderRadius: 'var(--ui-control-radius, 14px)',
    paddingRight: 12,
    paddingLeft: 12,
    backgroundColor: colors.accent,
    backgroundImage: 'none',
    color: colors.onAccent,
    cursor: 'default',
    fontSize: 12,
    fontWeight: 600,
    outline: 'none',
    boxShadow: {
      'default': colors.controlShadow,
      ':focus-visible': `0 0 0 2px ${colors.focus}`,
    },
    transform: {
      'default': 'scale(1)',
      ':active': 'scale(0.97)',
    },
    transitionDuration: {
      'default': '100ms',
      '@media (prefers-reduced-motion: reduce)': '0ms',
    },
    transitionProperty: 'background-color, box-shadow, transform',
    transitionTimingFunction: 'ease-out',
  },
})
