import * as stylex from '@stylexjs/stylex'

export const shelfSourceDialogStyles = stylex.create({
  sheetLayer: {
    position: 'fixed',
    zIndex: 100,
    inset: 0,
    display: 'grid',
    alignItems: 'center',
    justifyItems: 'center',
  },
})
