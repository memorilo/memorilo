import * as stylex from '@stylexjs/stylex'

export const appStyles = stylex.create({
  editorPage: {
    display: 'flex',
    width: '100%',
    height: '100vh',
    minHeight: 0,
    overflow: 'hidden',
  },
})
