app-id: dev.memorilo.desktop
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: memorilo
separate-locales: false

finish-args:
  - --device=dri
  - --share=ipc
  - --share=network
  - --socket=pulseaudio
  - --socket=wayland
  - --socket=fallback-x11
  - --talk-name=org.freedesktop.secrets
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons

modules:
  - name: memorilo
    buildsystem: simple
    build-commands:
      - cp -a linux-unpacked /app/memorilo
      - install -Dm755 memorilo.sh /app/bin/memorilo
      - install -Dm644 dev.memorilo.desktop.desktop /app/share/applications/dev.memorilo.desktop.desktop
      - install -Dm644 dev.memorilo.desktop.metainfo.xml /app/share/metainfo/dev.memorilo.desktop.metainfo.xml
      - install -Dm644 dev.memorilo.desktop.png /app/share/icons/hicolor/128x128/apps/dev.memorilo.desktop.png
    sources:
      - type: dir
        path: ../dist/linux-unpacked
        dest: linux-unpacked
      - type: file
        path: memorilo.sh
      - type: file
        path: dev.memorilo.desktop.desktop
      - type: file
        path: dev.memorilo.desktop.metainfo.xml
      - type: file
        path: dev.memorilo.desktop.png
