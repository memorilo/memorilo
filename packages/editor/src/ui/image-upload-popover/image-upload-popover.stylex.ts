import * as stylex from '@stylexjs/stylex'

export const imageUploadPopoverStyles = stylex.create({
  card: {
    display: 'flex',
    width: 384,
    flexDirection: 'column',
    gap: 16,
    borderRadius: 12,
    padding: 24,
    fontSize: 14,
  },
})
