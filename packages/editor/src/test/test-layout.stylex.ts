import * as stylex from '@stylexjs/stylex'

export const testLayoutStyles = stylex.create({
  blockHandleOffset: {
    marginLeft: 100,
  },
  fixedEditorViewport: {
    display: 'flex',
    height: 360,
  },
})
