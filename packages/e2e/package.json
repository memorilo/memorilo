{
  "name": "@memorilo/e2e",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test:e2e": "pnpm --filter @memorilo/desktop pack:dir && playwright test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "electron": "43.2.0"
  }
}
