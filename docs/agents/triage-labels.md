# Triage Labels

The skills speak in terms of five canonical triage roles. With the local-markdown tracker, triage state is recorded as a `Status:` line near the top of each issue file.

| Canonical role    | Status string      | Meaning                                  |
| ----------------- | ------------------ | ---------------------------------------- |
| `needs-triage`    | `needs-triage`     | Maintainer needs to evaluate this issue  |
| `needs-info`      | `needs-info`       | Waiting on reporter for more information |
| `ready-for-agent` | `ready-for-agent`  | Fully specified, ready for an AFK agent  |
| `ready-for-human` | `ready-for-human`  | Requires human implementation            |
| `wontfix`         | `wontfix`          | Will not be actioned                     |

When a skill mentions a role, use the corresponding `Status:` string in the issue file.

Edit the right-hand column to match whatever vocabulary you actually use.
