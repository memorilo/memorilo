{
  "worktree": {
    "setup": "nix develop -c pnpm install && nix develop -c pnpm --filter @memorilo/desktop exec electron --version && nix develop -c pnpm --filter @memorilo/desktop exec electron-vite build"
  }
}
